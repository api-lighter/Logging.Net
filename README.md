# Smart logging abstractions layer.

Might be very useful for business processes logging, data masking and errors tracing.


## Requirements:

	* log to stdout
	* log to file
	* log caller information
	* log business context and system context
	* log in json format
	* mask log values